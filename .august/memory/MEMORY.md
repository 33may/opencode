# August Memory

- 2026-05-29: AugustTask legacy compatibility: `task list --project <legacy name>` and `taskdb list` must resolve project filters by normalized key first, then exact stored `project.name`, so migrated invalid projects keyed as `LEGACY_N` remain addressable by original free-form names. Service issue moves should explicitly verify the target project with `getProject` before calling repository move logic.
- 2026-05-29: CodeGraph MVP shipped on worktree `feat/codegraph-mvp`: CLI-first `codegraph refresh|find|context|impact`, August-owned module under `packages/opencode/src/august/codegraph.ts`, index path `.august/codegraph/index.json`, deliberately no prompt injection/model-facing tools yet to keep upstream surface small.
- 2026-05-30: Validation harness hard assertions now support workspace artifact checks via `expected.files_exist` and `expected.files_contain` (`relative/path::needle`, case-insensitive contains). `augusttrainer-fake-ml` uses these to require notebook/result artifacts in addition to transcript/tool-call needles.
- 2026-05-30: Fixed `augusttrainer-fake-ml` failure roots: AugustTrainer validation-mode prompt now preserves exact final sentinels, and scenario JSONL assertions avoid strict numeric formatting by checking stable run/field tokens instead of `0.600000`.
- 2026-05-30: Tightened second `augusttrainer-fake-ml` failure: validation mode explicitly overrides overnight continuation, forbids extra hypotheses when bounded to one baseline plus one hypothesis, and stops after artifacts. The scenario removed the brittle final-sentinel assertion and validates the real behavior through artifacts, `0.600000`, and JSONL `quality_bonus`/`metric` checks.
- 2026-05-30: AugustTrainer MVP shipped on `feat/augusttrainer-autoresearch`: native `augusttrainer` primary agent, `scripts/augusttrainer` launcher, docs under `features/augusttrainer/`, validation harness file assertions, and fake ML scenario. It targets generic ML projects, creates project-local `augusttrainer/` lab notebooks, discovers train command/metric/editable/protected files, runs autonomously overnight, and may install dependencies only as logged/revertible hypotheses.
- 2026-05-30: User preference/correction: skip rerunning `./scripts/validate-scenario features/validation/scenarios/augusttrainer-fake-ml.yaml` when finishing/pushing; it can hang/no-load in this environment. Do not block integration on that scenario unless explicitly requested.
- 2026-05-30: `scripts/august` must preserve the caller cwd for the `aug` alias while still starting Bun from `packages/opencode` so Bun reads opencode's TSX config (`jsxImportSource: @opentui/solid`). Running the absolute TS entrypoint from a foreign cwd makes Bun default TSX to React and fail on `react/jsx-dev-runtime`. Wrapper pattern: capture caller cwd, `cd packages/opencode`, then eval `process.chdir(caller); process.argv.splice(1, 0, "src/index.ts"); await import("./src/index.ts")`.
- 2026-05-30: PMKK BC replay demo lives in worktree `/Users/may/.config/superpowers/worktrees/pmkk/bc-load-for-train`; use conda env `pmkk` or `/opt/homebrew/Caskroom/miniconda/base/envs/pmkk/bin/...`. Demo flow is data replay on `:9021`, `pmkk-bc-runner --mode=replay` with checkpoint `augusttrainer/runs/h5-no-test-leak-005/best.pt`, and `pm-monitor` via `cargo run --release --bin monitor-rs -- --asset btc --data ws --ws ws://127.0.0.1:9021`.
- 2026-05-31: User preference: use subagents for isolated debugging subtasks when the subtasks are independent, so each investigation has clean context and can run without blocking unrelated work.
- 2026-05-31: `maylenovo` is the Ubuntu/Tailscale server for always-on PM/Kalshi data collection, reachable on the tailnet as `maylenovo` with Tailscale IP `100.78.231.60`; use it for server-side capture/process checks over SSH.
- 2026-05-31: PM/Kalshi branch policy: `maymain` is May's own main/integration branch for the shared `pm_arb_bot` repo and should be the deploy branch for `maylenovo`; `hedge-rl-sim` was an experiment/runtime branch and should be retired after its work is merged into `maymain`.
- 2026-05-31: PM/Kalshi data architecture decision: Rust owns hot capture (`data_capture` raw/market SQLite and `cross_arb --bc-collect` bot SQLite), Python in `pm_arb_bot/tools/august_archive` owns archive/upload/restore/verify to private HF datasets using zstd Parquet, and PMKK is a consumer for replay/training from archived Parquet. This is acceptable near-term architecture because it separates low-latency capture from batch archival, but long-term CI/CD should treat the archive schema as a cross-repo contract between `pm_arb_bot` writer and PMKK reader.
- 2026-06-01: PM/Kalshi production deploy branches on `maylenovo`: `pm_arb_bot` tracks `origin/maymain`, `pmkk` now has GitHub default/production branch `origin/main`, and `pm-monitor` tracks `origin/rl-monitor`. The server runs `pm-stack-deploy-agent.timer` every 60s from `pm_arb_bot/tools/deploy/pm_stack_deploy.py`, building/testing changed targets before updating deploy state and restarting only relevant services.
- 2026-06-03: AugustResearch MVP implemented on branch `feat/augustresearch-stack`: native primary `augustresearch` agent, `scripts/augustresearch` launcher, docs under `features/augustresearch/`, project-local `augustresearch/` notebook artifacts, repo research tool exposure by effective permission, and validation scenario `features/validation/scenarios/augustresearch-bootstrap.yaml`. Scenario passes with hard artifact checks and judge pass; package `bun typecheck` remains blocked by unrelated untracked dictation test errors in the current working tree.
